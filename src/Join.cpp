#include "../incl/cmd/Join.hpp"

Join::Join(ft::Client *client, std::vector<ft::Channel *> &channels){
			if (check_params(client)){
				// if (check_existence(channels, int(channels.size())))
					// join channel
				// else
					// create channel
			}
				
		}